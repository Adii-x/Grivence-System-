const u=['urgent','emergency','unsafe','violence','harassment','danger','fire'];const h=['broken','not working','malfunction','leakage','wifi down'];module.exports={inferFinalPriority:({title,description,studentPriority})=>{const t=`${title} ${description}`.toLowerCase();if(u.some(k=>t.includes(k))) return 'Urgent';if(h.some(k=>t.includes(k))) return 'High';return studentPriority==='Medium'?'Medium':'Low';}};
